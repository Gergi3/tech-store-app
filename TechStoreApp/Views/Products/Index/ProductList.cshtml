@model ProductListViewModel
<div id="product-list-content-container">
    <div class="tp-shop-top mb-45">
        <div class="row">
            <div class="col-xl-6 align-self-end">
                <div class="tp-shop-top-left d-flex">
                    <div class="tp-shop-top-result">
                        <p>
                            Showing <span id="showingFrom">@Model.Pagination.ShowingFrom</span>
                            – <span id="showingTo">@Model.Pagination.ShowingTo</span>
                            of <span id="totalCount">@Model.Pagination.TotalCount</span> results
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    @if (Model.Items.Count > 0)
    {
        <partial name="Index/_Pagination" model="@Model.Pagination" />
    }

    <div class="tp-shop-items-wrapper tp-shop-item-primary">
        <div class="tab-content" id="productTabContent">
            <div class="tab-pane fade show active" id="grid-tab-pane" role="tabpanel" aria-labelledby="grid-tab" tabindex="0">
                <div class="row infinite-container">
                    @foreach (var product in Model.Items)
                    {
                        <partial name="Index/_ProductItem" model="@product" />
                    }
                </div>
            </div>
        </div>
    </div>

    @if (Model.Items.Count > 0)
    {
        <partial name="Index/_Pagination" model="@Model.Pagination" />
    }
</div>


@if (Model.Layout)
{
    <script type="module" src="~/js/pages/products-list.js"></script>
}
